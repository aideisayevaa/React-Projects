import React from 'react'

const City = ({ city }) => {

    // console.log(city)
    return (
        <div>
            clslijdc
            {/* <h1>{cityy.main.temp}</h1>
            <h1>{cityy.name}</h1>
            <h1>{cityy.weather[0].main}</h1> */}
        </div>
    )
}

export default City
